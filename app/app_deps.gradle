
dependencies {

    compile files('libs/httpmime-4.1.1.jar')

    compile "${SUPPORT_DEPS.supportV4}"
    compile "${SUPPORT_DEPS.supportV13}"
    compile "${SUPPORT_DEPS.appcompatV7}"
    compile "${SUPPORT_DEPS.cardview}"
    compile "${SUPPORT_DEPS.annotations}"
    compile "${SUPPORT_DEPS.design}"
    compile "${SUPPORT_DEPS.recyclerView}"

    compile "${DEPS.gif_drawable}"
    compile "${DEPS.hlistview}"

    compile "${GOOGLE_AGERA.content}"
    compile "${GOOGLE_AGERA.database}"
    compile "${GOOGLE_AGERA.net}"
    compile "${GOOGLE_AGERA.rvadapter}"
    compile "${GOOGLE_AGERA.rvdatabinding}"

}