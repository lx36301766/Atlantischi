
dependencies {

    api files('libs/httpmime-4.1.1.jar')

    api "${support_deps.supportV4}"
    api "${support_deps.supportV13}"
    api "${support_deps.appcompatV7}"
    api "${support_deps.cardview}"
    api "${support_deps.annotations}"
    api "${support_deps.design}"
    api "${support_deps.recyclerView}"

    api "${view.gif_drawable}"
    api "${view.hlistview}"

    api "${google_agera.content}"
    api "${google_agera.database}"
    api "${google_agera.net}"
    api "${google_agera.rvadapter}"
    api "${google_agera.rvdatabinding}"

    api "${annotation.butterknife}"
    annotationProcessor "${annotation.butterknife_compiler}"

    api 'pl.atlantischi.ximagebriage:library:0.0.1'


    api "${log.timber}"
    api "${log.klog}"
    api "${log.xlog}"
    api "${log.logger}"

    api 'org.jetbrains:annotations:13.0'
}