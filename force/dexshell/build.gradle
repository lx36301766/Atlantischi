apply plugin: 'java'

apply from: rootDir.absolutePath + "/gradle_config/kotlin_deps.gradle"

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"