apply plugin: 'java'

apply from: rootDir.absolutePath + "/gradle/kotlin_java.gradle"

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
