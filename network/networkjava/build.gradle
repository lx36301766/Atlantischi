
apply from: rootDir.absolutePath + "/gradle/java_config.gradle"

apply from: rootDir.absolutePath + "/gradle/deps_config.gradle"

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    compile "${network.retrofit2}"
    compile "${network.retrofit2_gson_convert}"
    compile "${network.retrofit2_fastjson_convert}"
    compile "${network.retrofit2_adapter_rxjava2}"
    compile "${network.okhttp3}"

    compile "${json.fastjson}"

//    compile "${rx.rxjava}"
    compile "${rx.rxjava2}"

}
